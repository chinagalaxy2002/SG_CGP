defaults:
  - default
  - _self_

accelerator: gpu
devices: 1
min_steps: 2800  # prevents early stopping
max_steps: 2800  # steps to train for
max_epochs: 50  # epochs to train for
gradient_clip_val: 0.1 # gradient clipping value
